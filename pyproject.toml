[project]
name = "maps-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "curl-cffi>=0.15.0",
    "pandas>=3.0.3",
    "pyyaml>=6.0.3",
    "scrapling>=0.4.8",
]
